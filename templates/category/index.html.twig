{% extends 'base.html.twig' %}

{% block title %}List of categories{% endblock %}

{% block body %}

<h1>List of categories</h1>
<a href="{{ path('new_category') }}" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm sm:w-auto px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800 inline-block">Add a category</a>

{% for category in categories %}
    <a href="{{ path('show_category', {'id': category.id}) }}">{{ category }}</a>
{% endfor %}

{% endblock %}
