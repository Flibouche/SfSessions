{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<div class="w-full h-screen overflow-x-hidden border-t flex flex-col">
  <main class="w-full flex-grow p-6">
    <h1 class="w-full text-3xl text-black mb-5 dark:text-white">Welcome home {{ app.user }} 😎👋</h1>

    <div class="relative overflow-x-auto shadow-md sm:rounded-lg mb-10">
      <h2 class="w-full text-2xl text-black mb-2 dark:text-white">Upcoming Sessions</h2>
      <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
        <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
          <tr>
            <th scope="col" class="w-1/6 px-6 py-3">
              Sessions <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Modules <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Duration <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Nb places <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Statut <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Actions <i class="fa-solid fa-arrow-down"></i>
            </th>
          </tr>
        </thead>
        <tbody>
          {% for session in upcomingSessions %}
          <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
            <th scope="row" class="px-6 mr-10 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
              <a href="{{ path('show_session', {'id': session.id}) }}">{{ session }}</a>
            </th>
            <td class="px-6 py-4">
              <p>Nb modules à définir</p>
            </td>
            <td class="px-6 py-4">
              <p>{{ session.startDate | date("d.m.Y") }} - {{ session.endDate | date("d.m.Y") }}</p>
            </td>
            <td class="px-6 py-4">
              <p>{{ session.nbPlaces }}</p>
            </td>
            <td class="px-6 py-4">
              {% if session.nbPlaces == 0 %}
              <p>CLOSED</p>
              {% else %}
              <p>OPEN</p>
              {% endif %}
            </td>
            <td class="flex px-6 py-4 gap-3">
              <a href="{{ path('edit_session', {'id': session.id}) }}"><i class="fa-solid fa-pen"></i></a>
              <a href="{{ path('delete_session', {'id': session.id}) }}"><i class="fa-solid fa-trash-can"></i></a>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

    <div class="relative overflow-x-auto shadow-md sm:rounded-lg mb-10">
      <h2 class="w-full text-2xl text-black mb-2 dark:text-white">Current Sessions</h2>
      <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
        <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
          <tr>
            <th scope="col" class="w-1/6 px-6 py-3">
              Sessions <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Modules <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Duration <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Nb places <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Statut <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Actions <i class="fa-solid fa-arrow-down"></i>
            </th>
          </tr>
        </thead>
        <tbody>
          {% for session in currentSessions %}
          <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
            <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
              <a href="{{ path('show_session', {'id': session.id}) }}">{{ session }}</a>
            </th>
            <td class="px-6 py-4">
              <p>Nb modules à définir</p>
            </td>
            <td class="px-6 py-4">
              <p>{{ session.startDate | date("d.m.Y") }} - {{ session.endDate | date("d.m.Y") }}</p>
            </td>
            <td class="px-6 py-4">
              <p>{{ session.nbPlaces }}</p>
            </td>
            <td class="px-6 py-4">
              {% if session.nbPlaces == 0 %}
              <p>CLOSED</p>
              {% else %}
              <p>OPEN</p>
              {% endif %}
            </td>
            <td class="flex px-6 py-4 gap-3">
              <a href="{{ path('edit_session', {'id': session.id}) }}"><i class="fa-solid fa-pen"></i></a>
              <a href="{{ path('delete_session', {'id': session.id}) }}"><i class="fa-solid fa-trash-can"></i></a>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

    <div class="relative overflow-x-auto shadow-md sm:rounded-lg mb-10">
      <h2 class="w-full text-2xl text-black mb-2 dark:text-white">Past Sessions</h2>
      <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
        <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
          <tr>
            <th scope="col" class="w-1/6 px-6 py-3">
              Sessions <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Modules <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Duration <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Nb places <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Statut <i class="fa-solid fa-arrow-down"></i>
            </th>
            <th scope="col" class="w-1/6 px-6 py-3">
              Actions <i class="fa-solid fa-arrow-down"></i>
            </th>
          </tr>
        </thead>
        <tbody>
          {% for session in pastSessions %}
          <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
            <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
              <a href="{{ path('show_session', {'id': session.id}) }}">{{ session }}</a>
            </th>
            <td class="px-6 py-4">
              <p>Nb modules à définir</p>
            </td>
            <td class="px-6 py-4">
              <p>{{ session.startDate | date("d.m.Y") }} - {{ session.endDate | date("d.m.Y") }}</p>
            </td>
            <td class="px-6 py-4">
              <p>{{ session.nbPlaces }}</p>
            </td>
            <td class="px-6 py-4">
              {% if session.nbPlaces == 0 %}
              <p>CLOSED</p>
              {% else %}
              <p>OPEN</p>
              {% endif %}
            </td>
            <td class="flex px-6 py-4 gap-3">
              <a href="{{ path('edit_session', {'id': session.id}) }}"><i class="fa-solid fa-pen"></i></a>
              <a href="{{ path('delete_session', {'id': session.id}) }}"><i class="fa-solid fa-trash-can"></i></a>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

  </main>
</div>

{% endblock %}